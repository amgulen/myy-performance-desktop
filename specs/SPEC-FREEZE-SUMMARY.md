# 需求冻结摘要 V1.0

**文档版本：** V1.0
**冻结日期：** 2026-04-24
**状态：** 已冻结（Frozen）

---

## 一、本版本覆盖范围

### 软件形态
- Windows桌面应用程序（Electron + React + TypeScript）
- SQLite本地数据库
- 解压即用绿色软件，GitHub Release分发

### 绩效模型（7类，含2类变体）
| 模型 | 说明 |
|---|---|
| 比例模型（固定比例） | 分类金额×固定比例，如挂号费80%、一人一方20% |
| 比例模型（动态比例/积分制） | 积分×(绩效总额÷总积分)，积分单价动态计算 |
| 固定金额模型 | 按人次/次数/份数发放固定金额，如夜班费、住院病历费 |
| 计件模型 | 数量×单价×岗位系数，如检验科各类项目计件 |
| 阈值后计提模型 | 达到基准线后仅对超出部分计提，如非药物特色治疗超额部分 |
| 评分考核模型 | 多指标加权评分×等级系数，如出诊专家综合考核 |
| 扣罚/质量控制模型 | 投诉、报告错误等扣减绩效，如报告错误、质控不合格 |

### 数据来源
- **ABC数字医疗云开放平台**（接口文档入口：https://open-region2.abcyun.cn/，待分析）
- **Excel导入**（当前主要可用方式）
- **手工录入**（辅助功能）
- 不假设HIS/LIS/收费系统存在

### 功能模块（第一阶段MVP）
| 模块 | 功能 |
|---|---|
| 项目初始化 | 首次启动配置、数据路径选择、数据库初始化 |
| 数据库 | SQLite本地存储 |
| 员工管理 | 科室管理、员工管理、岗位分类、人员信息 |
| 规则配置 | 创建/编辑/版本管理绩效规则（固定比例/动态比例/固定金额/计件/阈值/评分/扣罚） |
| 模拟数据 | 支持导入Excel模拟数据用于计算测试 |
| 绩效计算 | 一键计算，支持全部7种绩效场景，支持多规则叠加 |
| 结果展示 | 汇总视图、明细视图、计算路径追溯视图 |
| 结果控制 | 允许人工调整，保留原始结果，记录调整日志；支持结果锁定 |
| Excel导出 | 汇总表导出、明细表导出 |
| 备份恢复 | 手动备份、自动备份、导入恢复 |

### 配置能力
- 所有绩效规则（比例/金额/权重/阈值/等级/扣罚）均可配置
- 规则版本管理，支持随时修改
- AppId/AppSecret本地配置，AppSecret本地加密存储

---

## 二、不包含范围

以下内容在本版本中不实现，留待后续阶段：

| 内容 | 原因 |
|---|---|
| ABC接口实际接入 | 第一阶段仅做接口框架预留，文档尚未分析 |
| HIS/LIS/收费系统接口 | 医院当前无这些系统，不作为设计前提 |
| 实时数据同步 | 数据来源未确认 |
| 多用户/网络版 | 本版本确认单机单用户 |
| 并发控制 | 同上 |
| BI分析/数据可视化大屏 | 第一阶段MVP不含此内容 |
| 手机端 | 同上 |
| 审计日志 | 制度要求落地后补充 |
| Web版/Linux版 | 需求定位为Windows桌面软件 |

---

## 三、已确认核心决策

| # | 决策 | 说明 |
|---|---|---|
| 1 | 绩效结果允许人工调整 | 保留原始计算结果+调整记录 |
| 2 | 不强制人工确认环节 | 简化流程：计算→查看结果→导出 |
| 3 | 支持结果锁定 | 防止误改 |
| 4 | Excel导入为当前主要可用方式 | 系统根据配置自动生成模板 |
| 5 | 手工录人为辅助功能 | 使用频率低但必须支持，不限制数据量 |
| 6 | 积分制归入比例模型变体 | 不作为独立模型类型，设计阶段避免重复 |
| 7 | 制度调整频率不做限制 | 系统支持随时修改规则 |
| 8 | 规则版本管理必须支持 | 避免历史结果被污染 |
| 9 | AppSecret本地加密 | 不上传云端 |
| 10 | 单机单用户 | 不支持多用户并发 |

---

## 四、下一阶段目标（设计阶段）

### 进入设计阶段前必须完成

| 前置条件 | 说明 |
|---|---|
| ABC接口文档分析 | 读取 https://open-region2.abcyun.cn/ 逐页学习接口文档，整理字段映射 |
| Excel导入模板定义 | 业务方确认导入表头字段 |
| Critical Questions确认 | 组织业务方确认Q1-Q9（含绩效结果确认流程、数据冲突优先级等） |

### 设计阶段交付物（待规划）

1. 数据库表结构设计（SQLite）
2. 规则引擎数据模型设计
3. 技术架构设计（Electron+React+TypeScript+SQLite）
4. 模块接口设计
5. ABC接口层设计（预留）

---

## 五、需求变更流程

后续需求变更必须通过以下流程：

1. 提出变更申请（CR），说明变更内容、原因、影响范围
2. 评审通过后，更新版本号（如V1.0→V1.1）
3. 重新冻结文档

> 当前冻结版本：**V1.0**
> 冻结日期：2026-04-24
