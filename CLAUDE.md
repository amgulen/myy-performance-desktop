# 蒙医云绩效管理桌面系统 - 开发规范

---

## 一、项目阶段口径

当前项目仍处于进入编码前的需求与设计阶段。未得到明确指令前，不允许初始化项目、不允许创建src目录、不允许创建package.json、不允许编写业务代码。

V1正式交付目标包括：

- ABC接口接入与数据同步
- Excel导入兜底
- 手工录入补充
- 规则配置
- 绩效计算
- 结果追溯
- 报表导出
- 本地备份
- Windows打包发布

Excel导入可以作为开发测试期验证手段，但V1正式交付版必须完成ABC接口接入与数据同步，否则不能视为完整可用版本。

---

## 二、V1角色与权限边界

V1定位为Windows本地单机版：

- 不做角色权限控制
- 不做多用户
- 不做账号体系
- 不做登录体系
- 所有系统操作默认由单一管理员完成

财务、医生、科室负责人、人事相关人员等在V1中不是系统登录角色，只是报表使用方、业务参与方或制度确认方。

后续如果升级为云端/多用户/网络版，需要另行设计账号、角色、权限和审计体系。

---

## 三、最高优先级原则

### 1. 不允许写死规则

任何以下内容禁止写死：

- 提成比例
- 评分权重
- 阈值
- 固定金额
- 绩效等级
- 扣罚金额
- 岗位系数

全部必须配置化，并通过规则版本和生效时间控制。

### 2. 规则引擎优先

系统核心不是UI，而是规则引擎和计算引擎。规则定义、规则匹配、规则执行、结果生成、追溯记录必须作为核心能力设计。

### 3. 所有计算必须可追溯

必须支持：

- 数据来源
- 原始单号/业务单号
- 使用规则
- 规则版本
- 计算路径
- 人工调整记录

### 4. core必须独立

V1虽然是Electron桌面应用，但核心业务逻辑不能绑定Electron。必须设计独立core模块承载：

- 数据标准模型
- 规则引擎
- 计算引擎
- ABC字段映射
- Excel导入导出模型
- 计算追溯模型

Electron只作为桌面壳和本地能力承载层。未来迁移到Ubuntu、Docker、Web前端、后端API和云端数据库时，应尽量复用core。

---

## 四、统一绩效原子数据原则

ABC接口、Excel导入、手工录入的数据都不能直接进入绩效计算。所有来源数据必须先转换为统一的绩效原子数据，再进入计算流水线。

绩效原子数据至少应表达：

- 人员
- 科室
- 项目
- 项目分类
- 数量
- 金额
- 发生时间
- 数据来源
- 原始单号/业务单号
- 是否冲减/是否作废
- 可追溯来源

---

## 五、标准计算流水线

绩效计算必须按以下顺序设计和实现：

1. 数据导入/同步：ABC接口、Excel导入、手工录入
2. 数据归一化：转换为统一绩效原子数据
3. 数据去重与冲突处理
4. 退费/作废/退药等冲减处理
5. 规则匹配：根据人员、科室、岗位、项目分类、时间匹配规则
6. 基础绩效计算：比例、固定金额、计件等
7. 阈值/基准线规则处理
8. 评分模型处理
9. 扣罚/质量控制处理
10. 人工调整
11. 结果锁定
12. 报表导出和备份

后续规则引擎、数据模型、计算过程追溯都必须围绕该流程设计。

---

## 六、技术路线

V1暂定技术路线：

- Electron
- React
- TypeScript
- SQLite
- Windows解压即用
- GitHub Release发布

禁止在V1引入：

- 云数据库
- 服务器强依赖
- 多用户并发体系
- 手机端
- 财务远程登录
- 医生端

---

## 七、核心模块边界

### 1. core模块

负责业务核心能力，必须平台无关。

### 2. 数据访问层

负责SQLite本地存储、查询、备份恢复相关访问能力。

### 3. ABC适配层

负责ABC接口配置、鉴权、连接测试、字段映射、数据同步、同步日志、失败重试和对账辅助。

### 4. Excel导入导出层

负责模板生成、Excel解析、导入校验、导出报表。Excel数据同样必须先归一化为绩效原子数据。

### 5. UI层

负责页面展示和本机交互，不承载核心计算逻辑。

---

## 八、禁止行为

禁止：

- 写死业务规则、比例、金额、权重、阈值
- 用大量if/else堆叠制度逻辑
- 让ABC、Excel、手工录入数据绕过绩效原子数据模型直接计算
- 将规则引擎、计算引擎绑定到Electron窗口或React组件
- 在未完成设计文档前直接进入编码
- 擅自扩大V1范围到云端、多用户、医生端、财务远程登录

---

## 九、编码前必须完成的设计文档

进入编码前至少应完成并确认：

1. 技术架构设计文档
2. 规则引擎设计文档
3. 数据模型设计文档
4. 计算流程设计文档
5. MVP开发任务拆解文档

---

## 十、目标

本系统目标是长期可用、可配置、可追溯、可迁移的本地绩效管理系统，而不是一次性工具。
